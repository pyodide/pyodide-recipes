Emscripten 6.0.3 added OpenMP support but we don't support threads
diff --git a/setup.py b/setup.py
--- a/setup.py
+++ b/setup.py
@@ -173,6 +173,7 @@
                 "--disable-maintainer-mode",
                 "--without-zlib-check",
                 "--without-fortran",
+                "--disable-openmp",
             ]
 
             log.info("%s", " ".join(cmd))
